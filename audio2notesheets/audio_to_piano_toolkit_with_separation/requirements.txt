# Audio to Piano Toolkit with Separation
# Install with: pip install -r requirements.txt

# Separation engines (choose one or both)
demucs>=4.0.0
# spleeter>=2.4.0  # Uncomment if using Spleeter (may require separate venv)

# Core transcription & post-processing libraries
basic-pitch>=0.2.0
pretty_midi>=0.2.10
librosa>=0.10.1
soundfile>=0.12.1
numpy>=1.23
mido>=1.3.2

# Optional dependencies
music21>=9.0.0  # For score generation
crepe>=0.0.12  # For monophonic transcription
piano_transcription_inference>=0.1.0  # For piano-specific transcription

# Audio processing
ffmpeg-python>=0.2.0

# ML/Deep Learning (if not installed with above packages)
torch>=1.11.0
torchaudio>=0.11.0

# Utilities
pathlib
argparse